import { SumWorkhoursPipe } from './sum-workhours.pipe';

describe('SumWorkhoursPipe', () => {
  it('create an instance', () => {
    const pipe = new SumWorkhoursPipe();
    expect(pipe).toBeTruthy();
  });
});
